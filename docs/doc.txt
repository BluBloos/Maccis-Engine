struct key_state
{
  bool endedDown;
  unsigned int halfTransitionCount;
}

endedDown:
Set to 1 if the key was down last frame, and set to 0 otherwise.

halfTransitionCount:
The count of half transitions recorded during the previous sub frame interval. A full transition is LOW-HIGH-LOW signal.
